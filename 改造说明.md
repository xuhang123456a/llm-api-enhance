# llm-api-enhance 改造说明

面向「多个桌面端 Agent 共用同一个模型基座做横向对比」的场景，本仓库在上游基础上做了四组改动。

---

## 一、新增 `${session_id}` 占位符：按对话生成会话标识

### 为什么需要

OpenCode Go 要求客户端为**每段对话**发送一个稳定的会话 ID（`x-opencode-session`）。桌面端工具不会发这个头，必须在代理层补齐。上游仓库原本只支持 `${ORIGIN}` / `${UUID}` / `${TIMESTAMP}`，其中 `${UUID}` 每次请求都变，不满足「同一段对话保持稳定」。

### 实现

| 文件 | 改动 |
|---|---|
| `internal/pipeline/session.go` | 新增。从请求体推导会话标识 |
| `internal/pipeline/interpolate.go` | 新增 `sessionToken`，插值函数扩展为 `interpolateWithSession` |
| `internal/pipeline/headers.go` | 请求头插值时传入 `plan.SessionID` |
| `internal/pipeline/pipeline.go` | 请求时把推导出的会话标识填入 `HeaderPlan` |
| `internal/planner/plan.go` | `HeaderPlan` 增加 `SessionID` 字段 |
| `internal/config/loader.go` | **关键修复**：把 `session_id` 加入运行时变量白名单 |

### 会话标识如何推导

取请求体 `messages[]` 里**第一条 `role == "user"` 的消息内容**，做 SHA1，取前 24 位十六进制，加 `sess-` 前缀。

之所以取首条 user 消息，是因为它在整段对话中**永不改变**——后续轮次只是在数组尾部追加，所以同一段对话的每一轮都会算出同一个值。不同对话的首条问题不同，自然区分开。

兼容三种请求格式：
- OpenAI / Anthropic：`messages[].content`（字符串或内容块数组）
- Gemini：`contents[].parts[].text`

解析失败时回退为对整个请求体做哈希（至少保证同一次请求内部自洽）。

### 踩过的坑（重要）

上游的配置加载器会把配置文件里所有 `${VAR}` 当作**环境变量**展开。`session_id` 不在运行时变量白名单里，于是 `${session_id}` 被展开成空串——请求头静默丢失，且没有任何报错。

排查过程：`Authorization` 和 `User-Agent` 都注入成功，唯独 `x-opencode-session` 整个头不见，说明配置阶段就被吃掉了。修复即在 `internal/config/loader.go` 的 `isRuntimeVariableName` 中加上 `session_id`。

---

## 二、多访问密钥 → 工具标签：按工具归因

### 为什么需要

所有工具共用同一个代理后，上游只看到「一个客户端」，无法回答「这条请求是 WorkBuddy 发的还是 Qoder 发的」。而横评恰恰需要按工具统计 token 消耗与耗时。

### 配置

```yaml
security:
  access_key: bench-local-key          # 默认密钥，标签为 "default"
  access_keys:                          # 每个工具一个独立密钥
    - {key: bench-workbuddy-01, label: WorkBuddy}
    - {key: bench-qoder-01,     label: Qoder}
```

各工具填写：

```
Base URL : http://127.0.0.1:8888/bench-workbuddy-01/proxy/opencode-go/v1
API Key  : bench-workbuddy-01
```

校验规则：密钥长度须大于 8；密钥不可重复（含与 `access_key` 重复）；标签不可重复。任一违反都会在启动时直接报错。

涉及文件：`internal/config/types.go`、`validate.go`、`snapshot.go`（`LabelForAccessKey`）、`internal/router/proxy_handler.go`、`internal/planner/plan.go`（`RouteInfo.ToolLabel`）。

---

## 三、请求/响应转录（JSONL）

### 为什么需要

横评最有说服力的素材是「各工具实际发了什么」——系统提示词注入、上下文压缩策略、是否附带工具定义，这些只有把原始请求抓下来才看得见。同时 token 用量是「谁更省」这一结论的唯一依据。

### 配置

```yaml
transcript:
  enable: true
  path: ./data/transcripts.jsonl
  capture_bodies: true        # 是否记录请求/响应原文
  max_body_bytes: 262144      # 单次记录中请求体、响应体各自的捕获上限
```

> ⚠️ `capture_bodies: true` 会把各工具的完整提示词落盘，属敏感数据，注意保管。默认关闭。

### 记录字段

```json
{
  "time": "2026-09-23T15:59:16.6+08:00",
  "request_id": "req-1790150356605144200",
  "tool": "WorkBuddy",
  "channel": "opencode-go",
  "model": "deepseek-v4.1-flash",
  "session_id": "sess-91e2a4d793206b20386ac2ae",
  "status": 200,
  "stream": false,
  "elapsed_ms": 3,
  "usage": {"prompt_tokens": 10, "completion_tokens": 20, "total_tokens": 30},
  "request_bytes": 88,
  "response_bytes": 589,
  "request_body": "...",
  "response_body": "..."
}
```

`tool` + `session_id` 两个字段组合起来，就能按「工具 × 对话」任意聚合。

### token 用量提取

`internal/transcript/capture.go` 兼容三种上游格式：

| 格式 | 字段 |
|---|---|
| OpenAI | `usage.prompt_tokens` / `completion_tokens` / `total_tokens` |
| Anthropic | `usage.input_tokens` / `output_tokens` |
| Gemini | `usageMetadata.promptTokenCount` / `candidatesTokenCount` / `totalTokenCount` |

流式响应的用量出现在**最后一个 SSE 数据块**，因此捕获缓冲除了保留开头（用于记录正文），还额外维护一个 16 KB 的**尾部滑动窗口**专门用于提取用量——否则长响应被截断后就拿不到 token 数了。

### 上游失败也会记录

连接失败、超时、隐私处理失败都会落一条记录（`status: 0` 或实际状态码，附 `error` 字段）。否则统计会漏掉失败的调用，导致「某工具跑得特别快」的假象。

---

## 四、配套工具

### `tools/echo_server.py` — 本地回显服务

把收到的请求头与请求体原样吐回，用于在不消耗真实额度的情况下验证头注入是否正确。响应里附带一个 `usage` 块，使它能一并验证用量提取链路。

```bash
python tools/echo_server.py 8899
```

### `tools/verify_session.sh` — 一键端到端自检

需要回显服务监听 8899、代理以 `config.echo-test.yaml` 监听 8898，然后运行：

```bash
bash tools/verify_session.sh
```

覆盖四项断言：同一对话会话标识稳定、不同对话互相区分、跳过 system 消息取首条 user、会话标识带 `sess-` 前缀。最后自动打印按工具汇总的 token 表。

### `tools/summarize_transcript.py` — 按工具汇总

```bash
python tools/summarize_transcript.py data/transcripts.jsonl
```

输出示例：

```
工具         请求数  对话数  输入  输出  合计  平均耗时  失败
-------------------------------------------
WorkBuddy  3    1    30  60  90  3ms   0
Qoder      1    1    10  20  30  2ms   0

总 token 用量：120    总请求数：4
```

---

## 五、顺带修复的上游缺陷

这三处与本次需求无关，但会让 `go test ./...` 在 Windows 上变红，影响判断「改动是否引入回归」，因此一并修掉。

### 1. `configtest.yaml` 缺失

`internal/app/app_test.go` 依赖仓库根目录的 `configtest.yaml`，但上游仓库没有这个文件（`.gitignore` 也并未忽略它），导致 `internal/app` 整个包测试失败。已按测试断言补全该夹具。

### 2. Windows 路径导致 JSON 解码失败

`internal/router/router_test.go` 用字符串拼接构造请求体：

```go
strings.NewReader(`{"path_type":"local","path":"` + path + `"}`)
```

Windows 的临时路径形如 `C:\Users\...\bad-123.yaml`，拼进 JSON 后 `\U` 是**非法转义**，服务端解码失败并返回「path_type and path are required」。在 macOS / Linux 上路径用正斜杠，所以这个 bug 不会暴露。

已改为 `json.Marshal` 构造（抽出 `reloadRequestBody` 辅助函数），两处调用点统一。

---

## 六、测试

```bash
GOPROXY=https://goproxy.cn,direct go test ./...
```

当前状态：**全部通过**。

本次新增的测试：

| 文件 | 覆盖内容 |
|---|---|
| `internal/pipeline/session_test.go` | 会话标识稳定性、对话区分、跳过 system、Anthropic/Gemini 格式、`${session_id}` 插值 |
| `internal/pipeline/record_test.go` | 转录落盘字段、注入头与转录一致、跨轮次会话稳定、上游失败也记录、无转录时正常转发 |
| `internal/transcript/transcript_test.go` | 三种格式的用量提取、SSE 用量、截断后仍能提取用量、JSONL 追加不覆盖、nil Sink 安全 |
| `internal/config/config_test.go` | `${session_id}` 保留、多密钥校验、标签映射、转录配置校验 |

---

## 七、免账号接入 Codex 与 Claude Code

**结论先说：你不需要 Codex 账号，也不需要 Claude 账号。**

这不是绕过什么限制，而是这套架构本来就不依赖它们的账号——模型基座来自 OpenCode Go，Codex 和 Claude Code 在这里只是**客户端外壳**（harness）。它们唯一需要的能力是「能指向自定义 base_url」，而两者都支持。

### Codex（复用你已有的 Codex++）

Codex++（开源项目 `BigPizzaV3/CodexPlusPlus`）是 Codex 桌面端的**外部增强启动器**，通过注入方式解锁「纯 API 模型」模式——这正是免账号的关键。它自带 Manager 可配置供应商，**支持自定义 Base URL 和 API Key**，能直接指向我们的代理。

1. 打开 **Codex++ Manager** → 左侧「供应商配置」→ 新增
2. 按下表填写：

| 参数 | 填什么 |
|---|---|
| 供应商名称 | `bench-local` |
| 接入模式 | **纯 API 模型**（关键，不要选官方账号模式） |
| 模型 | `deepseek-v4.1-flash` |
| Base URL | `http://127.0.0.1:8888/bench-codex-01/proxy/opencode-go/v1` |
| API Key | `bench-codex-01` |
| 混入 API Key | 勾选 |
| 上游协议 | 先选 **Chat Completions** |

3. 保存 → 点「重启 Codex」
4. 启动后确认右下角模型下拉里出现 `bench-local / deepseek-v4.1-flash`

**上游协议怎么选**：先试 Chat Completions（兼容性最好）；若 Codex 报协议不匹配再换 Responses。我们的代理是路径透传的，两个端点分别原样转到上游的 `/v1/chat/completions` 和 `/v1/responses`。

### Claude Code（无需 Anthropic 账号，且不需要任何第三方启动器）

**和 Codex 的关键区别**：Codex 桌面端原生有账号门禁，所以需要 Codex++ 这种注入式启动器绕过；**Claude Code 原生就支持指向自定义端点**，配环境变量即可，不需要任何第三方补丁。

这一点对你的横评很重要——**Claude Code 是干净的 harness，没有注入污染**，可以直接作为基线。

#### 终端版（推荐，这才是 agent 形态）

```bash
# 1. 前置：Node.js 18+ 与 Git
node -v && npm -v

# 2. 安装
npm install -g @anthropic-ai/claude-code
claude --version
```

```bash
# 3. 环境变量（Windows cmd/PowerShell 用 setx，Git Bash 用 export）
setx ANTHROPIC_BASE_URL "http://127.0.0.1:8888/bench-claude-01/proxy/opencode-go-anthropic"
setx ANTHROPIC_AUTH_TOKEN "bench-claude-01"
setx ANTHROPIC_MODEL "deepseek-v4.1-flash"
setx ANTHROPIC_DEFAULT_OPUS_MODEL "deepseek-v4.1-flash"
setx ANTHROPIC_DEFAULT_SONNET_MODEL "deepseek-v4.1-flash"
setx ANTHROPIC_DEFAULT_HAIKU_MODEL "deepseek-v4.1-flash"
```

- `ANTHROPIC_BASE_URL` **不要带 `/v1`**，Claude Code 会自己拼 `/v1/messages`
- 四个 MODEL 变量都要设，否则 Claude Code 会按 opus/sonnet/haiku 三档去请求上游不存在的模型名
- **如果环境里已有 `ANTHROPIC_API_KEY`，先清掉**（`setx ANTHROPIC_API_KEY ""` 或 Git Bash 里 `unset`），否则它会覆盖你的配置

#### ⚠️ 全新安装必须做的一步：跳过引导

**光配环境变量不够。** 新版 Claude Code 在全新安装时会强制走一遍引导/登录流程，即使环境变量已配好也会弹。

编辑 `C:\Users\<用户名>\.claude.json`（Git Bash：`~/.claude.json`），在 JSON 顶层加一个字段：

```json
{
  "installMethod": "unknown",
  "autoUpdates": true,
  "projects": {},
  "hasCompletedOnboarding": true
}
```

已有内容就合并，**别整份覆盖**。注意前一个字段后面要补英文逗号。

> 如果该文件不存在，先跑一次 `claude` 让它自动生成，退出后再改。

#### 验证

```bash
claude
```

进去后：

```
/status     # 核对接口地址是你的代理地址
/model      # 核对默认模型是 deepseek-v4.1-flash
```

若 `/status` 显示的还是官方地址，说明环境变量没生效——`setx` 需要**重开终端**，或者你是在同一个终端里逐行敲的（环境变量要一次性设置完再启动）。

#### 🚫 不要装那个「专用适配包」

网上教程（包括 CSDN 上流传很广的那篇）会让你先跑一个 `./install.sh` 适配包，说是"改写请求逻辑以兼容第三方 API"。

**你不需要它，而且必须避开它。**

那个补丁是给「中转站只会说 OpenAI 协议」的场景用的。我们的代理**原生就说 Anthropic 协议**（`/v1/messages`），直连即可。装那个包等于往你的被测 harness 里注入一层未知改动——正是横评最忌讳的变量污染。

#### 桌面端（可选）

Claude 桌面版也支持不登录接第三方：

1. 菜单栏 **Help → Troubleshooting → Enable Developer mode**
2. **Open Settings → Developer → Configure third-party inference**
3. 类型选 **Gateway**，填 Base URL 与 API Key
4. 若模型列表为空，在该界面右上角「Default → 在文件管理器中打开」，手动补字段后重启：

```json
{
  "inferenceProvider": "gateway",
  "inferenceGatewayBaseUrl": "你的代理地址",
  "inferenceGatewayApiKey": "你的密钥",
  "inferenceModels": ["deepseek-v4.1-flash"]
}
```

> 注意：桌面端这个 Gateway 设置期望的协议格式我没有实测确认（示例给的是 OpenRouter 这类 OpenAI 兼容地址）。如果填 anthropic 渠道报协议错误，改填 openai 渠道的地址 `http://127.0.0.1:8888/bench-claude-01/proxy/opencode-go/v1` 再试。
>
> 另外，桌面端主要用于对话；**要测 agent 能力还是用终端版**。

> 若上游报鉴权失败，把 `opencode-go-anthropic` 渠道的 `Authorization: Bearer` 换成 `x-api-key` 再试。

### ⚠️ 必须知道：Codex++ 是你实验里的一个变量

这是本节最需要提醒的一点。

Codex++ 通过**注入脚本**改变 Codex 行为。也就是说，你在视频里叫它「Codex」，但实际被测对象是 **「Codex + Codex++ 注入」**。如果它改了系统提示词、加了内置工具、或调整了请求参数，测出来的就不是原生 Codex。

这不是「不能做」，而是**必须在视频里说清楚**——否则一旦有观众看出来，会质疑整期结论的严谨性。

好在转录功能正好能验证这件事。转录里记录了 Codex++ 发出的**原始请求体**，把它和官方 Codex CLI（纯 `~/.codex/config.toml` 配置、零注入）的请求体对比，就能看出 Codex++ 到底注入了什么。

**这反而是个好素材——你有能力证明它动了什么，而别人只能猜。**

建议的处理方式（三选一）：

1. **如实标注**：说明 Codex 端用了 Codex++ 启动器，并展示对比证据。最诚实，信息量也最大。
2. **换纯 CLI 做对照**：Codex CLI 可只用 `~/.codex/config.toml` 配自定义 provider，零注入，是干净的 Codex 基线。
3. **两者都测**：原生 Codex CLI vs Codex++ 版，多一个「同一 harness 不同封装」的对比维度。

Claude Code 没有这个问题——它靠环境变量配置，不注入，是干净的。

---

## 八、使用步骤

```bash
# 1. 设置上游密钥
export OPENCODE_GO_KEY="你的 OpenCode Go 密钥"

# 2. 构建
go build -o ai-api-stronger.exe ./cmd

# 3. 启动
./ai-api-stronger.exe -config ./config.opencode-go.yaml

# 4. 各客户端按各自的密钥接入（见第二节、第七节）
```

各客户端填写的 Base URL（同一上游、同一模型，只是协议不同）：

| 客户端 | 渠道 | Base URL | API Key |
|---|---|---|---|
| WorkBuddy / Qoder / ZCode / DSH / Codex | `opencode-go` | `http://127.0.0.1:8888/<自己的key>/proxy/opencode-go/v1` | 见 `access_keys` |
| Claude Code | `opencode-go-anthropic` | `http://127.0.0.1:8888/bench-claude-01/proxy/opencode-go-anthropic` | `bench-claude-01` |

```bash
# 5. 跑完一轮后看汇总
python tools/summarize_transcript.py data/transcripts.jsonl
```

实测两个渠道都正常：`/api/health` 返回 `channel_count: 2`，两个端点请求均抵达上游。

---

## 九、已知限制

- **转录配置不支持热重载**：它持有文件句柄，只在启动时读取。改了 `transcript` 段需重启。
- **多密钥不支持热重载后的标签变更**：`access_keys` 属于快照内容，热重载会生效；但转录的文件目标不会。
- **请求体不做改写**：`config.opencode-go.yaml` 里刻意留空了 `model_rewrite` / `system_prompt` / `max_tokens` / `temperature`，因为横评要测的就是各工具的原始请求。若要做归一化对照实验，可以再开一套配置。
- **`${session_id}` 依赖首条 user 消息**：若某个工具每轮都重写历史（而非追加），会话标识会随之变化。这是设计取舍，不是缺陷。
